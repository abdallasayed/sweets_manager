name: sweets_manager
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # التاريخ والأرقام
  intl: ^0.19.0
  # قاعدة البيانات المحلية (سريعة جداً)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # التعامل مع ملفات الإكسل
  excel: ^4.0.0
  # الوصول لملفات الهاتف والمشاركة
  path_provider: ^2.1.2
  share_plus: ^9.0.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

